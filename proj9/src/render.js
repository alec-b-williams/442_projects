class RenderMesh {
  constructor(matrix, mesh) {
    this.matrix = matrix;
    this.mesh = mesh;
  }
}

class RenderLight {
  constructor(pos, color) {
    this.pos = pos;
    this.color = color;
  }
}